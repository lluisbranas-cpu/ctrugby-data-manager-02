import type { Metadata } from "next";
export const metadata: Metadata = { title: "CTRugby · Federaciones" };
export default function FederationsLayout({ children }: Readonly<{ children: React.ReactNode }>) { return children; }
