import type { Metadata } from "next";
export const metadata: Metadata = { title: "CTRugby · Clubs" };
export default function ClubsLayout({ children }: Readonly<{ children: React.ReactNode }>) { return children; }
